# Kakao Developers > app > platform keys > JavaScript key.
# Copy this file to .env.local, fill in the value, then restart npm run dev.
# Register http://localhost:3000 (and http://127.0.0.1:3000 if used) as SDK domains.
NEXT_PUBLIC_KAKAO_MAP_KEY=
